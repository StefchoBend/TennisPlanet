@using Microsoft.AspNetCore.Identity
@using TennisPlanet.Infrastructure.Data.Domain
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
<style>
    .nav-item {
        font-size: 17px;
        font-weight: 500
    }
    .nav-item button {
        font-size: 17px;
        font-weight: 500;
    }

</style>
@* <ul class="navbar-nav">
    <li class="nav-item" style="padding-left:2rem; font-size:2rem; padding-right:2rem">
        <a class="nav-link text-light" asp-area="" asp-controller="Wishlist" asp-action="Index"><img src="/images/wishlist-icon-2048x1952-13b2gake.png" style="height:3rem;"</a>
    </li> 
</ul> *@
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-controller="Wishlist" asp-action="Index">Wishlist</a>
    </li>
}
<ul class="navbar-nav">
    @if (this.User.Identity.IsAuthenticated)
    {
        <li class="nav-item">
            <a class="nav-link text-black" asp-area="" asp-controller="Order" asp-action="MyOrders"><i class="bi bi-cart4"></i></a>
        </li>
    }
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity?.Name!</a>
    </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}
</ul>
